Changes needed for AlipayPayment.jsx:

1. Add email state after realName:
   const [email, setEmail] = useState("");

2. Add phone validation function after handlePlatformChange:
   const validatePhoneFormat = (phone) => {
     // Format: 233-000000000 (9 digits) or 233-0000000000 (10 digits)
     const phoneRegex = /^233-\d{9,10}$/;
     return phoneRegex.test(phone);
   };

3. Update proceedToPaymentInstructions validation to add:
   - After alipayAccount check, add:
     if (accountType === "personal") {
       if (!validatePhoneFormat(alipayAccount)) {
         toast.error("Personal account must be in format: 233-000000000 or 233-0000000000");
         return;
       }
       if (!email.trim()) {
         toast.error("Email is required for personal accounts");
         return;
       }
       // Basic email validation
       const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
       if (!emailRegex.test(email)) {
         toast.error("Please enter a valid email address");
         return;
       }
     }

4. Add email field in the form after Real Name input (around line 665)

5. Add email to paymentData object in handleSubmit
