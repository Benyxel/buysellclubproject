// Auth token key names used in localStorage or cookies
export const ACCESS_TOKEN = 'accessToken';

// Keep both spellings to satisfy existing imports
export const REFRESH_TOKEN = 'refreshToken';
export const REFRSH_TOKEN = REFRESH_TOKEN;

// Optional admin token keys (if referenced elsewhere)
export const ADMIN_TOKEN = 'adminToken';
export const USER_TOKEN = 'token';


